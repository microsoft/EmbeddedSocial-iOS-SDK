# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MSGP' do
  use_frameworks!
  
  pod 'Alamofire', '~> 4.5'

  target 'MSGPTests' do
    inherit! :search_paths
    
  end

end
