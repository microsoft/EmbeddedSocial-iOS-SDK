//
// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See LICENSE in the project root for license information.
//

import XCTest

//Uncomment and run to test and debug API Mock

class Loop: UITestBase {
    
    override func setUp() {
        super.setUp()
        
    }
    
    override func tearDown() {
        super.tearDown()
    }
    
    func testAPIMock() {
        while true {}
//        
//        let app = XCUIApplication()
//        app.collectionViews.buttons["Edit profile"].tap()
//        
//        let tablesQuery = app.tables
//        tablesQuery.cells["First Name"].children(matching: .textField).element.tap()
//        
//        let tablesQuery2 = tablesQuery
//        tablesQuery2.textFields.containing(.button, identifier:"Clear text").element.typeText("вы")
//        tablesQuery.cells["Last Name"].children(matching: .textField).element.tap()
//        
        
//        let app = XCUIApplication()
//        app.navigationBars["EmbeddedSocial.NavigationStackContainer"].children(matching: .button).element(boundBy: 0).tap()
//        app.children(matching: .window).element(boundBy: 1).children(matching: .other).element.children(matching: .other).element(boundBy: 1).children(matching: .other).element.children(matching: .other).element(boundBy: 1).tap()
//        app.collectionViews.buttons["Edit profile"].tap()
//        app.navigationBars["Edit profile"].buttons["Save"].tap()
        //        let clearTextTextField = tablesQuery2.textFields.containing(.button, identifier:"Clear text").element
//        clearTextTextField.tap()
//        clearTextTextField.typeText("ава")
//        
//        let textView = tablesQuery.cells["Bio"].children(matching: .textView).element
//        textView.tap()
//        textView.tap()
//        textView.typeText("папа")
//        
    }
    
}
